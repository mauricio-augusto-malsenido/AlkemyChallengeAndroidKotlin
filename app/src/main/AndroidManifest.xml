<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.alkemy.alkemychallengeandroidkotlin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/peliculas"
        android:label="@string/app_name"
        android:roundIcon="@drawable/peliculas"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlkemyChallengeAndroidKotlin">
        <activity
            android:name=".View.DetallePeliculaActivity"
            android:exported="false" />
        <activity
            android:name=".View.PeliculasActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>